<div class="mt-4">
  <h3>Khóa học đã xóa</h3>
  <a href="/me/trash/courses">Thùng rác</a>
  <table class="table mt-4">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Tên khóa học</th>
        <th scope="col" colspan="2">Mô tả</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
        {{#each courses}}
      <tr>
        <th scope="row">{{sum @index 1}}</th>
        <td>{{this.name}}</td>
        <td>{{this.description}}</td>
        <td><a href="/courses/{{this._id}}/edit" class="btn btn-link">Sửa</a></td>
        <td><a href="" class="btn btn-link" data-id = {{this._id}} data-toggle="modal" data-target="#course-delete">Xóa</a></td>
      </tr>
      {{else}}
      <tr>
        <td colspan = "5" class="text-center">
          Bạn chưa xóa khóa học nào cả. Hãy xóa đi nào! 
          <a href="/me/stored/courses">Xóa khóa học</a> 
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>

{{!-- Model Delete --}}
<div id="course-delete" class="modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Xóa khóa học?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Bạn có chắc chắn muốn xóa khóa học này?</p>
      </div>
      <div class="modal-footer">
        <button id="btn-delete-course" type="button" class="btn btn-danger">Xóa</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Hủy</button>
      </div>
    </div>
  </div>
</div>

{{!-- Delete hidden Form --}}
<form method="POST" id="form-course-delete"></form>

<script>

  document.addEventListener("DOMContentLoaded", function() {
  var courseId;
  var deleteForm = document.forms['form-course-delete'];
  var btnDeleteCourse = document.getElementById('btn-delete-course');

  // When dialog confirm click
  $('#course-delete').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget) 
  courseId = button.data('id') ;
  console.log("🚀 ~ file: stored-courses.hbs ~ line 51 ~ id", courseId)
  });

  btnDeleteCourse.onclick = function(){
      deleteForm.action = '/courses/' + courseId + '?_method=DELETE';
      deleteForm.submit();
  }
})

</script>